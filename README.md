# Test RoR

The steps to start the application.

* `bundle install`
install gems

* `rails db:create`
Create DB.

* `rails db:migrate`
Run migrations.

* `rails s`
Start server (localhost:3000).

* `rails test`
Start test.
